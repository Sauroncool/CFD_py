1. To run the script (24M0003.py), you need to have the following libraries installed:
   - numpy
   - matplotlib
   - scipy

2. You can install these libraries using pip:
   ```
   pip install numpy matplotlib scipy
   ```
3. It will generate the plots and save them in the active directory, and 
   will display the data in the console.
