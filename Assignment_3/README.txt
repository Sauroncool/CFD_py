1. To execute the 24M0003.py script, the user must have Python installed on their machine. The user must also have the following libraries installed: numpy and matplotlib. The user can install these libraries by running the following command in the terminal: pip installnumpy matplotlib

2. The User should either craete the directory Results or the code will create the directory for the user (verified on Windows 11 and Linux Mint).

3. All the results will be saved in the Results directory.
