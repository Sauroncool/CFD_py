1. Ensure that numpy and matplotlib are installed on your system. If not, install them using the following commands:
```
pip install numpy
pip install matplotlib
```
2. Run the code using the following command:
```
python 24M0003.py
```
3. The code will generate a plot of the grid obtained using TFI and save it as a PNG file named "generated_grid.png" in the same directory.
